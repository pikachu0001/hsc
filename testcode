writeln math.pi * 10
writeln 2 + 	3
(* wow that's
a comment*) write "that's a \""
writeln 'string"'
writeln 5 + math.e
write -2 * -4
(* another one! 

*)


writeln
var x : real
var pippo:boolean
pippo := 10
writeln 66
writeln pippo + pippo * 20
var pippo:string
halt
writeln 22